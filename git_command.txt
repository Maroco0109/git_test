// add
git add {filename}
git add .

// commit
git commit -m '{memo}'

// branch
git branch {branch_name}
git switch {branch_name}
git merge {branch_name}

// if git conflict: 같은 파일, 같은 줄 수정 하면
1. 원하는 코드만 남기고
2. git add
3. git commit